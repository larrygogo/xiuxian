# 使用指南

## ✅ Phase 2 完成 - 认证与角色创建

### 已实现的功能

#### 1. 登录场景 (LoginScene)
- ✅ 完整的登录/注册UI
- ✅ 输入验证（用户名3-20字符，密码至少6字符）
- ✅ 登录/注册模式切换
- ✅ 错误提示显示（3秒自动消失）
- ✅ Enter键快捷提交
- ✅ API集成（authAPI.login/register）
- ✅ Token自动保存到localStorage
- ✅ WebSocket自动连接
- ✅ 根据游戏状态智能跳转

**UI元素**：
- 标题：问道长生
- 副标题：Phaser 3 版本
- 输入框：仙号、密令
- 按钮：登录、注册、切换模式
- 错误提示区域

**特性**：
- 移动端友好的DOM输入框
- 触摸反馈动画
- 加载状态管理（防止重复提交）

#### 2. 角色创建场景 (CharacterCreateScene)
- ✅ 修仙主题UI设计
- ✅ 角色名称输入（2-10字符）
- ✅ 输入验证
- ✅ 装饰性动画元素（⚛ ✨ 🌟 ☯ ⚡）
- ✅ 角色预览动画（呼吸效果）
- ✅ API集成（gameAPI.createCharacter）
- ✅ 成功提示动画
- ✅ 自动跳转到主界面

**UI元素**：
- 标题：创建仙道之路
- 说明文字
- 角色预览（圆形头像 + 图标）
- 输入框：仙号
- 按钮：开始修仙
- 装饰元素（旋转动画）

#### 3. 预加载场景优化 (PreloadScene)
- ✅ Token验证
- ✅ 自动获取游戏状态
- ✅ WebSocket自动连接
- ✅ 智能场景跳转：
  - 无token → LoginScene
  - 有token但无角色 → CharacterCreateScene
  - 有token且有角色 → MainScene
  - Token失效 → 清除状态 → LoginScene

## 🎮 测试流程

### 1. 启动服务器

确保后端服务器运行在 http://localhost:3000

```bash
# 在game/server目录
cd ../server
npm run dev
```

### 2. 启动Phaser客户端

```bash
# 在game/phaser-client目录
pnpm run dev
```

访问: **http://localhost:5174**

### 3. 测试注册流程

1. 打开浏览器访问 http://localhost:5174
2. 应该看到登录界面
3. 点击"没有账号？去注册"
4. 输入用户名（3-20字符）和密码（至少6字符）
5. 点击"注册"按钮
6. 注册成功后自动跳转到角色创建界面
7. 输入角色名称（2-10字符）
8. 点击"开始修仙"
9. 创建成功后显示成功消息，然后跳转到主界面

### 4. 测试登录流程

1. 如果已有账号，在登录界面直接输入用户名和密码
2. 点击"登录"按钮
3. 登录成功后：
   - 如果有角色：跳转到主界面
   - 如果没有角色：跳转到角色创建界面

### 5. 测试刷新恢复

1. 登录成功后，刷新页面
2. 应该自动恢复登录状态
3. 如果token有效，直接进入对应界面
4. 如果token失效，返回登录界面

## 🐛 常见问题

### Q: 输入框点击后没有反应？
A: UIInput使用DOM overlay实现，点击后会创建原生input元素。请检查浏览器控制台是否有错误。

### Q: 登录后提示401错误？
A: 检查后端服务器是否运行在 http://localhost:3000，以及CORS配置是否正确。

### Q: 注册后没有跳转？
A: 检查浏览器控制台，查看API响应。确认后端API返回了正确的token和user信息。

### Q: 输入框无法输入中文？
A: DOM input应该支持中文输入。如果有问题，请检查浏览器兼容性。

## 📱 移动端测试

### 在手机上测试

1. 确保手机和电脑在同一局域网
2. 修改Vite配置，允许网络访问：
   ```bash
   pnpm run dev -- --host
   ```
3. 使用电脑的IP地址访问，如：http://192.168.1.100:5174
4. 测试触摸操作：
   - 点击输入框应该弹出虚拟键盘
   - 按钮应该有触摸反馈
   - Enter键提交应该工作

### Chrome DevTools移动端模拟

1. 打开Chrome DevTools (F12)
2. 点击设备工具栏图标（Ctrl+Shift+M）
3. 选择移动设备（如iPhone 12 Pro）
4. 测试触摸交互

## 🔍 调试技巧

### 查看状态管理

在浏览器控制台：

```javascript
// 查看当前状态
import('@/services/managers/StateManager').then(({ stateManager }) => {
  console.log('Token:', stateManager.getToken());
  console.log('User:', stateManager.getUser());
  console.log('Game State:', stateManager.getGameState());
});
```

### 查看localStorage

```javascript
console.log('Token:', localStorage.getItem('token'));
console.log('User:', localStorage.getItem('user'));
```

### 清除缓存重新测试

```javascript
localStorage.clear();
location.reload();
```

## 📊 性能指标

- **首次加载**: < 1秒
- **场景切换**: < 200ms
- **API响应**: < 500ms（取决于网络）
- **动画帧率**: 60fps

## 🎨 UI规范

### 颜色
- Primary: #3498db
- Success: #2ecc71
- Warning: #f39c12
- Danger: #e74c3c
- Dark: #2c3e50
- Light: #ecf0f1

### 字体
- 标题: 48-64px, bold
- 正文: 16-20px
- 按钮: 20-22px

### 间距
- 按钮最小尺寸: 44x44px（移动端）
- 输入框高度: 40-45px
- 元素间距: 20-30px

## 下一步

接下来将实现 **Phase 3: 主界面与角色系统**

包括：
- 主游戏界面布局
- 角色状态栏（HP/MP/灵气进度条）
- 快捷操作按钮
- 事件日志面板
- 角色属性面板
- 升级和加点系统
