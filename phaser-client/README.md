# Phaser 3 游戏客户端

基于Phaser 3游戏引擎的修仙主题游戏客户端。

## 🚀 快速开始

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm run dev

# 构建生产版本
pnpm run build

# 预览生产版本
pnpm run preview
```

开发服务器地址: **http://localhost:5174**

## 📁 项目结构

```
phaser-client/
├── public/              # 静态资源
│   └── assets/         # 游戏美术资源
├── src/
│   ├── config/         # 配置文件
│   │   ├── game.config.ts    # Phaser游戏配置
│   │   ├── api.config.ts     # API端点配置
│   │   └── constants.ts      # 游戏常量
│   ├── scenes/         # 游戏场景
│   │   ├── BootScene.ts
│   │   ├── PreloadScene.ts
│   │   ├── LoginScene.ts
│   │   ├── CharacterCreateScene.ts
│   │   ├── MainScene.ts
│   │   ├── BattleScene.ts
│   │   └── UIScene.ts
│   ├── ui/             # UI组件系统
│   │   ├── core/       # 核心UI组件
│   │   ├── panels/     # 具体面板
│   │   ├── battle/     # 战斗UI
│   │   └── widgets/    # 小组件
│   ├── services/       # 业务服务
│   │   ├── api/        # API服务
│   │   ├── websocket/  # WebSocket服务
│   │   └── managers/   # 管理器
│   ├── systems/        # 游戏系统
│   ├── entities/       # 游戏实体
│   ├── types/          # 类型定义
│   └── utils/          # 工具函数
└── vite.config.ts      # Vite配置
```

## 🎮 技术栈

- **游戏引擎**: Phaser 3.90.0
- **语言**: TypeScript 5.9.3
- **构建工具**: Vite 5.4.21
- **UI插件**: phaser3-rex-plugins 1.80.17
- **WebSocket**: socket.io-client 4.8.3
- **HTTP客户端**: axios 1.13.3

## 🔧 核心组件

### 状态管理 (StateManager)
- Token管理
- 用户信息管理
- 游戏状态管理
- 事件总线

### UI组件库
- `UIContainer` - 容器基类
- `UIPanel` - 可拖拽面板
- `UIButton` - 按钮组件
- `UIInput` - 输入框组件
- `UIText` - 文本组件
- `UIProgressBar` - 进度条组件

### API服务
- `AuthAPI` - 认证服务（登录/注册）
- `GameAPI` - 游戏服务（角色、物品、升级）
- `BattleAPI` - 战斗服务（场景、房间、指令）

### WebSocket服务
- `GameSocket` - 游戏实时状态更新
- `BattleSocket` - 战斗实时事件

## 🌐 API配置

通过环境变量配置API地址：

```bash
# .env
VITE_API_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
```

## 📱 移动端支持

项目优先考虑移动端适配：

- **屏幕适配**: 1280x720基准分辨率，FIT缩放模式
- **触摸优化**: 最小触摸区域44x44px
- **拖拽支持**: 支持触摸拖拽
- **输入框**: DOM overlay实现，支持虚拟键盘

## 🎯 开发状态

### ✅ 已完成
- [x] 项目搭建和配置
- [x] 类型系统定义
- [x] 核心UI组件库
- [x] 状态管理系统
- [x] API服务层
- [x] WebSocket服务
- [x] 基础场景骨架

### 🚧 进行中
- [ ] 登录/注册界面
- [ ] 角色创建界面
- [ ] 主游戏界面
- [ ] 背包系统
- [ ] 战斗系统

## 📝 开发规范

### 命名规范
- **文件名**: PascalCase (如 `LoginScene.ts`)
- **组件类**: PascalCase (如 `class UIButton`)
- **函数/变量**: camelCase (如 `connectGame()`)
- **常量**: UPPER_SNAKE_CASE (如 `GAME_WIDTH`)

### 目录结构
- `scenes/` - 场景文件
- `ui/core/` - 可复用UI组件
- `ui/panels/` - 具体业务面板
- `services/` - 业务服务
- `types/` - TypeScript类型定义

## 🔗 与React客户端共存

两个客户端使用不同端口，可以同时运行：

- React客户端: `http://localhost:5173`
- Phaser客户端: `http://localhost:5174`
- 服务端: `http://localhost:3000`

共享localStorage中的token和用户信息，可以互相切换。

## 📄 License

MIT
